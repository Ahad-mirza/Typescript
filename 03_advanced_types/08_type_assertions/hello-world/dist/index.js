const element = document.getElementById('myInput');

// Since we don't have type assertion in JavaScript, we just assume that 'element' is an HTMLInputElement
console.log(element.value);  // Access the 'value' property directly (JavaScript will handle it dynamically)
